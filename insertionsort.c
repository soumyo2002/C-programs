#include<stdio.h>

void insertion_sort(int a[],int n)
{
    int j=1,k=0;
    for(int i=0;i<=(n-2);i++)
    {
        while(k>=0)
        {
            
        }
    }
}

int main()
{
    int arr[]={5,4,3,2,1};
    int n=5;
    insertion_sort(arr,n);
}